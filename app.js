const $=(id)=>document.getElementById(id);const sampleInput=$('sampleInput'),notesInput=$('notesInput'),noteContent=$('noteContent'),toast=$('toast');$('dateText').textContent=new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',year:'numeric'}).format(new Date());function showToast(message){toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2800)}function readImage(input,target){const file=input.files[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{target.src=reader.result;target.hidden=false};reader.readAsDataURL(file)}sampleInput.addEventListener('change',()=>{readImage(sampleInput,$('sampleThumb'));showToast('Handwriting sample added to this device.')});$('notesImageInput').addEventListener('change',(e)=>{if(e.target.files[0])showToast('Note photo added. Paste the text below to create the cleanest result.')});function bulletize(text){let lines=text.split(/\n+/).map(s=>s.trim()).filter(Boolean);if(lines.length===1)lines=lines[0].split(/(?<=[.!?])\s+/).filter(Boolean);return lines.map(line=>line.replace(/^[•\-*]\s*/,''))}$('generateButton').addEventListener('click',()=>{const input=notesInput.value.trim();if(!input){notesInput.focus();showToast('Add some notes first, then create your page.');return}const lines=bulletize(input),useBullets=$('bulletMode').checked;noteContent.innerHTML='';lines.forEach((line,i)=>{const p=document.createElement('p');p.textContent=(useBullets?'• ':'')+line;p.style.marginLeft=(i%3===1?3:0)+'px';noteContent.appendChild(p)});noteContent.style.color=$('inkColor').value;showToast('Your ruled-paper note is ready.')});$('downloadButton').addEventListener('click',()=>window.print());$('resetButton').addEventListener('click',()=>{sampleInput.value='';notesInput.value='';$('sampleThumb').hidden=true;noteContent.innerHTML='<p>• Photosynthesis is how plants make their food.</p><p>• It uses sunlight, water, and carbon dioxide.</p><p>• The plant gives off oxygen as a result.</p>';showToast('Ready for a fresh note.')});